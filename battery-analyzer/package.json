{
  "name": "i99dash-example-battery-analyzer",
  "version": "0.1.0",
  "private": true,
  "description": "Battery analysis + health dashboard — SoC, cell balance, thermal balance, charging power, lifetime stats with plain-English explanations and warning indicators.",
  "type": "module",
  "scripts": {
    "dev": "i99dash dev",
    "validate": "i99dash validate",
    "build": "i99dash build",
    "publish": "i99dash publish"
  },
  "devDependencies": {
    "i99dash": "^5.0.0"
  }
}
