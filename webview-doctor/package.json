{
  "name": "i99dash-example-webview-doctor",
  "version": "0.1.0",
  "private": true,
  "description": "Car WebView / bridge capability analyzer. The fallback diagnostic a user opens when another mini-app doesn't work; exports a privacy-clean .txt report to share with i99dash support on Telegram.",
  "type": "module",
  "scripts": {
    "bundle": "esbuild src/app.js --bundle --format=iife --target=es2019 --outfile=src/app.bundle.js",
    "dev": "pnpm bundle && i99dash dev",
    "validate": "i99dash validate",
    "build": "pnpm bundle && i99dash build",
    "publish": "pnpm bundle && i99dash publish"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "i99dash": "^5.2.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "keytar"
    ]
  }
}
