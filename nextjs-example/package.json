{
  "name": "i99dash-example-nextjs",
  "version": "0.3.1",
  "private": true,
  "description": "Reference mini-app — getContext, callApi, EN/AR + RTL, driving-state banner. Rebuilt from Next.js 15 to a single classic-IIFE esbuild bundle so the same bundle runs on Di5.0 (L5 / L5 Ultra / Song Plus) as well as Di5.1 (L8).",
  "type": "module",
  "scripts": {
    "bundle": "esbuild src/main.js --bundle --format=iife --target=es2019 --outfile=src/app.bundle.js",
    "dev": "pnpm bundle && i99dash dev",
    "validate": "i99dash validate",
    "build": "pnpm bundle && i99dash build",
    "publish": "pnpm bundle && i99dash publish"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "i99dash": "^5.0.0"
  }
}
